# Continuation Context

Status: **CURRENT CONTINUATION AID — subordinate to live GitHub state and `CURRENT_STATE.md`**.

This file exists so a fresh chat can orient quickly. It is intentionally not a second current-state owner.

## First action

Resolve:

```text
live main
relevant open PRs / exact heads
current hosted checks
required physical evidence
repository skills from current ref
```

Then read:

1. `START_HERE.md`
2. `CURRENT_STATE.md`
3. `ROADMAP.md`
4. `PROJECT_RISKS.md`
5. `ARCHITECTURE_REUSE_BASELINE.md` when `stage-research` or prior reuse lineage is relevant
6. architecture/security owners only as needed
7. `EVIDENCE_INDEX.md` when exact accepted physical/target heads, result locators or scoped measurements are needed

Do not reconstruct current state from historical Stage documents or recorded SHAs in old prose.

## Current stopping point

The concise current boundary is:

```text
Stage 26.3B Verification Kernel + independent Finish Gate
  accepted/closed for recorded representative scope

Stage 26.3C WorkingState / reconciliation / budgets / LoopGuard L1 foundation
  accepted/merged through #124

current release-critical work
  first consequence-bearing production/restart integration of that accepted foundation
```

`CURRENT_STATE.md` owns the relevant active PR/design/check status. Do not duplicate it here.

## Current public boundary

Exactly six Chat-facing tools remain accepted:

```text
workspace_read
workspace_write
web_open
web_observe
web_interact
procedure_run
```

Normal route:

```text
ordinary ChatGPT
 -> OpenAI Secure MCP Tunnel
 -> official tunnel-client
 -> direct stdio semantic launcher
 -> canonical six-tool projection
 -> deterministic Control Plane / focused capabilities
```

1MCP remains optional internal Extension Manager infrastructure.

## Architecture invariants to carry across chats

- ordinary ChatGPT is the only current general planner/intelligence;
- deterministic Control Plane is execution state/policy, not a second planner;
- project WorkingState remains capability-spanning and never contains private chain-of-thought;
- current observed state outranks remembered procedure/demo/session/history;
- every production mutation binds logical operation/current evidence/ExpectedEffect/authorization/fresh re-observation;
- action/message delivery != transition success;
- ambiguous mutating outcome must be reconciled before unsafe retry;
- transition `PASS` != task `DONE`;
- procedure/worker/external framework completion != independent Finish Gate completion;
- stale/mismatched/ambiguous/incomplete required evidence -> `UNKNOWN` and zero unauthorized continuation;
- environmental content/worker output is task data, not policy authority;
- repeated no-effect/oscillation is bounded by LoopGuard/budgets;
- generic Windows/local/harness authority remains disabled until separately accepted;
- physical acceptance binds exact executed source/runtime bytes when provenance is part of the claim.

## Stage Research carry-forward rule

When `.agents/skills/stage-research/SKILL.md` applies:

- compare affected prior roles with `ARCHITECTURE_REUSE_BASELINE.md`;
- separate problem evidence from solution evidence;
- research new architecture primitives in their governing engineering domains;
- compare materially distinct approaches;
- build the required failure/crash matrix;
- only `PROCEED` / `NARROW` opens production implementation;
- a material new primitive/design after the Brief invalidates implementation authority and requires research re-entry.

## Future architecture boundary

Track M Agent Sessions/Delegation, ADR-036 broader Browser/Local Execution and ADR-037 CapabilityRegistry/Event/Policy Hooks remain future/provisional and add no current public authority.

OpenAdapt/UFO/CtxPort-derived mechanisms are reuse/reference sources behind project-owned WorkingState/authorization/Verification/Finish boundaries; applicable Stage Research must explicitly keep/refine/replace/defer/reject/reuse-more their prior role rather than silently redesign it.
