relay.example.com {
    encode zstd gzip

    reverse_proxy 127.0.0.1:8787

    header {
        X-Content-Type-Options nosniff
        Referrer-Policy no-referrer
        -Server
    }

    log {
        output file /var/log/caddy/agent-platform-relay-access.log
        format json
    }
}
