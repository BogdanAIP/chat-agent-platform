{
  "contract_version": "capability-requirements-v1",
  "requirements": [
    {
      "capability": "media.inspect",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.ffmpeg"],
      "fallbacks": [],
      "acceptance": ["duration", "sample_rate", "channels", "integrated_lufs", "true_peak_dbtp"]
    },
    {
      "capability": "media.validate",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.ffmpeg"],
      "fallbacks": [],
      "acceptance": ["duration", "stream_inventory", "container_probe"]
    },
    {
      "capability": "media.convert",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.ffmpeg"],
      "fallbacks": [],
      "acceptance": ["lossless_wav", "lossless_flac", "output_probe", "artifact_sha256"]
    },
    {
      "capability": "media.extract_audio",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.ffmpeg"],
      "fallbacks": [],
      "acceptance": ["primary_audio_stream", "pcm_s24le", "output_probe"]
    },
    {
      "capability": "media.normalize_loudness",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.ffmpeg"],
      "fallbacks": [],
      "acceptance": ["two_pass_ebu_r128", "target_lufs_tolerance", "true_peak_ceiling", "output_probe"]
    },
    {
      "capability": "media.mux",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.ffmpeg"],
      "fallbacks": [],
      "acceptance": ["video_stream_present", "audio_stream_present", "output_probe", "artifact_sha256"]
    },
    {
      "capability": "audio.mastering_analyze",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.ffmpeg"],
      "fallbacks": [],
      "acceptance": ["ebu_r128_metrics", "profile_target", "explicit_quality_flags", "safe_auto_gate", "benchmark_corpus", "artifact_sha256"]
    },
    {
      "capability": "audio.mastering_produce",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.mastering"],
      "fallbacks": [],
      "acceptance": ["source_hash_idempotency", "persistent_job", "safe_auto_gate", "lossless_wav_master", "final_loudness_tolerance", "final_true_peak_ceiling", "duration_integrity", "artifact_sha256", "resume_without_duplicate_master"]
    },
    {
      "capability": "audio.reference_master",
      "required": false,
      "required_quality": "professional",
      "execution_paths": ["edge.python.matchering"],
      "fallbacks": [],
      "acceptance": ["target_reference_hash_idempotency", "pinned_matchering_e2e", "pcm24_intermediate", "technical_delivery_qc", "duration_integrity", "artifact_sha256", "resume_without_duplicate_master"]
    },
    {
      "capability": "audio.reaper_render",
      "required": true,
      "required_quality": "professional",
      "execution_paths": ["local.reaper"],
      "fallbacks": [],
      "acceptance": ["reaper_detected", "source_ffprobe_valid", "track_imported", "marker_created", "project_saved", "wav_rendered", "render_ffprobe_valid", "render_sample_rate_verified", "artifact_sha256"]
    }
  ]
}
