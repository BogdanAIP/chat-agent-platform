{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "policy-decision-v1.schema.json",
  "title": "Policy decision v1",
  "type": "object",
  "required": ["decision_id", "capability", "decision", "effective_risk", "enforced_by", "reasons"],
  "properties": {
    "decision_id": {"type": "string", "pattern": "^pol_[a-f0-9]+$"},
    "capability": {"type": "string", "minLength": 1},
    "decision": {"enum": ["allow", "guarded"]},
    "effective_risk": {"enum": ["low", "medium", "high", "critical"]},
    "enforced_by": {"type": "string", "minLength": 1},
    "reasons": {"type": "array", "items": {"type": "string"}},
    "confirmation_binding": {"type": "string", "pattern": "^confirm_[a-f0-9]{64}$"}
  },
  "additionalProperties": false
}
